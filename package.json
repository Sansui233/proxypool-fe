{
  "name": "proxypool-css",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss-watch": "sass --watch --no-source-map scss:css",
    "scss-build": "sass --no-source-map scss:css"
  },
  "devDependencies": {
    "bulma": "^0.9.1"
  }
}
